apply plugin: "java"

task buildStarterJar(type: Jar) {
	include "../app-info"
    archiveName = destinationDir.getPath().subString(destinationDir.getPath().indexOf("build/libs/") - 11) + "-Installer"
}
