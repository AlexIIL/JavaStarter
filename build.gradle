apply plugin: "java"

task buildStarterJar(type: Jar) {
    from "../app-info", "build/classes/**/*.class"
    archiveName = destinationDir.getPath().split("/")[6] + "-Installer.jar"
}

buildStarterJar.dependsOn build
